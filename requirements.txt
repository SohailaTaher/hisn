# HISN — Python Dependencies

# DNS & Network
dnspython>=2.6.0          # Pure Python DNS toolkit
python-whois>=0.9.0       # WHOIS lookups in Python
tldextract>=5.1.0         # Properly parse domain names

# HTTP & APIs
requests>=2.31.0          # The HTTP library
httpx>=0.27.0             # Modern async HTTP client

# CLI & Output
rich>=13.7.0              # Beautiful terminal output (colors, tables, progress bars)
typer>=0.12.0             # Modern CLI framework
click>=8.1.0              # CLI building blocks

# Configuration
python-dotenv>=1.0.0      # Load secrets from .env files

# Development
pytest>=8.0.0             # Testing framework


# Web framework (added Week 5)
fastapi>=0.115.0           # API framework
uvicorn[standard]>=0.32.0  # ASGI server with autoreload + websockets

# Database (added Week 5)
sqlmodel>=0.0.22           # ORM — Pydantic + SQLAlchemy in one

# Async task queue (added Week 5)
celery>=5.4.0              # Background task queue (used Day 2+)
redis>=5.2.0               # Celery broker + result backend (used Day 2+)Jinja2==3.1.6
weasyprint==69.0
bcrypt==5.0.0
email-validator==2.3.0
PyJWT==2.13.0
python-multipart==0.0.32
